# =============================================================================
# Copy to `.env` in the repo root. Never commit `.env`.
# Generate a production key: python -c "import secrets; print(secrets.token_urlsafe(32))"
# =============================================================================

# --- Required (server + client) ---
# Dev placeholder below — generate your own for real use.
MCP_API_KEY=dev_mcp_k7x9Qm2pLw4nR8vT3sY6hJ1fG5bN0cZ

# --- Weather tool (server) ---
# WEATHER_API_KEY=

# --- Client / Streamlit (optional) ---
# MCP_SERVER_URL=http://127.0.0.1:8000
# GROQ_API_KEY=
